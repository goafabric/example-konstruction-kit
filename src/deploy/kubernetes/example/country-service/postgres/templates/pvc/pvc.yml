apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${_Values_app_name}-pv-claim
  labels:
    app: ${_Values_app_name}
spec:
  storageClassName: "standard"
  capacity:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
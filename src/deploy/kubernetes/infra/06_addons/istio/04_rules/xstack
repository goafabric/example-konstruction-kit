#!/bin/bash
COMMAND=$1
PROFILE=$2

function up() {
    istioctl apply -f ./templates/tracing.yml
}

function down() {
    istioctl delete -f ./templates/tracing.yml
}


if   [ "${COMMAND}" = "init" ] ; then up;
elif [ "${COMMAND}" = "restart" ] ; then down && up;
elif [ "${COMMAND}" = "prune" ]; then down;

else echo Doing nothing !; fi
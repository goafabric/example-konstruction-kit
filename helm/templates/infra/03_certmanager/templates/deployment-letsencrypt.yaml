apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: letsencrypt-issuer
  namespace: cert-manager
spec:
  acme:
    # Staging API
#    server: https://acme-staging-v02.api.letsencrypt.org/directory
    server: https://acme-kubev02.api.letsencrypt.org/directory
    email: yourname@email.com
    privateKeySecretRef:
      name: account-key
    solvers:
      - http01:
          ingress:
            class: public

---

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: letsencrypt-ca
  namespace: cert-manager
spec:
  secretName: root-certificate
  issuerRef:
    name: letsencrypt-issuer
  dnsNames:
    - ${_Values_host_name}



{{- if .Values.ingress.enabled -}}
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: {{ include "application.fullname" . }}-0
spec:
  http:
    - name: {{ include "application.fullname" . }}-0
      match:
        hosts:
          - {{ .Values.ingress.hosts}}
        paths:
          - {{ .Values.ingress.paths }}/0
          - {{ .Values.ingress.paths }}/0/*
      backends:
        - serviceName: {{ include "application.fullname" . }}
          servicePort: http
      plugins:
        - name: redirect
          enable: true
          config:
            http_to_https: true
        - name: openid-connect
          enable: true
          config:
            client_id: oauth2-proxy
            client_secret: none
            discovery: https://{{ .Values.ingress.hosts}}/oidc/realms/tenant-0/.well-known/openid-configuration
            access_token_in_authorization_header: false
            set_id_token_header: false
            set_userinfo_header: false
            ssl_verify: false
            redirect_uri: "/core/0/redirect_uri"
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - {{ .Values.ingress.paths }}/0/(.*)
              - /$1
  {{- end }}
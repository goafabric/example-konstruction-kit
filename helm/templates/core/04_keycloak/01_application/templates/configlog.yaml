apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "application.fullname" . }}-config-log
  labels:
    {{- include "application.labels" . | nindent 4 }}
data:
  #log levels
  logging.level.root: "WARN"
  logging.level.com.cgm.de.ais: "INFO"